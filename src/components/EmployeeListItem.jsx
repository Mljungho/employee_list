import React from 'react'

const EmployeeListItem = ({ employee }) => {
    return (
        <React.Fragment>
            <item data-cy
            
        </React.Fragment>
    )
}

export default EmployeeListItem
